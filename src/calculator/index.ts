export function calcHouseMaterials(
    name:string,
     width:number, 
     length:number, 
     units:boolean)
     {
         console.log(name, width, length, units + " Called the function in /calculator/index.ts");
     }

export function getHouseMaterials(name:string)
{
    console.log(name + " Called the function in /calculator/index.ts");
}