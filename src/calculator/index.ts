export function calcHouseMaterials(
    name:string,
     width:number, 
     length:number, 
     units:boolean)
     {
         console.log(name, width, length, units + " Called the function");
     }